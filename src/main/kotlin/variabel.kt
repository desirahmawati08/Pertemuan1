fun main(args: Array<String>) {

    //tipe sistem yang dinamis tidak memerlukan adanya deklarasi
    //pada sebuah variabel, compiler aakan otomatis mengenali tipe berdasarkan nilai.

    var dynamicVar = 2
    println("Contoh dynamic variabel = " + dynamicVar)

    var staticVar: Char = 'A'
    println("Contoh static variabel = " + staticVar)


}